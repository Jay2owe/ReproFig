# Layer 8 of 9 - Signer trusted

The valid signature's public-key fingerprint satisfies an explicit local trust-store policy.

Required verification meanings: `internally_consistent`, `signature_valid`, `signer_trusted`.

Boundary: Trust is local to that policy, not a universal identity claim.

Open `preview/raw-user-input-comparison.png` for the visible figure, `verification/meaning-summary.csv` for the compact result, and `unpacked/` for the evidence recovered from the Scalable Vector Graphics carrier.
