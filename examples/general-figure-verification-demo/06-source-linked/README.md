# Layer 6 of 9 - Source linked

An executable declared transformation reconstructs the exact plotted table from the copied raw CSV.

Required verification meanings: `internally_consistent`, `source_linked`.

Boundary: This does not establish that the supplied observations were honestly collected.

Open `preview/raw-user-input-comparison.png` for the visible figure, `verification/meaning-summary.csv` for the compact result, and `unpacked/` for the evidence recovered from the Scalable Vector Graphics carrier.
