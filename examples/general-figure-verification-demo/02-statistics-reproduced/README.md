# Layer 2 of 9 - Statistics reproduced

The declared numbers match a rerun through the recorded producer-equivalent calculation route.

Required verification meanings: `internally_consistent`, `statistics_reproduced`.

Boundary: This is repeatability through the same route, not an independent implementation.

Open `preview/raw-user-input-comparison.png` for the visible figure, `verification/meaning-summary.csv` for the compact result, and `unpacked/` for the evidence recovered from the Scalable Vector Graphics carrier.
