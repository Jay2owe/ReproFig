# Layer 4 of 9 - Figure reproduced

The trusted producer reruns in a temporary workspace and saves a separate matching figure.

Required verification meanings: `internally_consistent`, `figure_reproduced`.

Boundary: Producer execution is explicit and does not prove the inputs were honestly collected.

Open `preview/raw-user-input-comparison.png` for the visible figure, `verification/meaning-summary.csv` for the compact result, and `unpacked/` for the evidence recovered from the Scalable Vector Graphics carrier.
