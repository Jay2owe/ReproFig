# Layer 5 of 9 - Display verified

The visible dots, means, comparison bracket and p-value text match their semantic bindings.

Required verification meanings: `internally_consistent`, `display_verified`.

Boundary: Unbound decoration is outside this check.

Open `preview/raw-user-input-comparison.png` for the visible figure, `verification/meaning-summary.csv` for the compact result, and `unpacked/` for the evidence recovered from the Scalable Vector Graphics carrier.
