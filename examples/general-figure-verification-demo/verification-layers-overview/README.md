# ReproFig verification layers overview

This timeline shows the traceable baseline and all nine formal verification meanings. Each row states the narrow claim established by a pass and the boundary that remains.

The exact displayed wording is in `data/der/figure_data.csv`; the standalone Matplotlib producer is `code/plot.py`.
