# Source index

`data/src/verification-layers.csv` is an exact copy of the verification-layer definition table at `input/verification-layers.csv`. Its byte size and SHA-256 digest are recorded in `sources.csv`.
