# Layer 3 of 9 - Statistics independently verified

ReproFig's reference Welch test matches a separately implemented Python-standard-library calculation.

Required verification meanings: `internally_consistent`, `statistics_independently_verified`.

Boundary: This verifies the declared calculation, not whether Welch's test was the right scientific choice.

Open `preview/raw-user-input-comparison.png` for the visible figure, `verification/meaning-summary.csv` for the compact result, and `unpacked/` for the evidence recovered from the Scalable Vector Graphics carrier.
