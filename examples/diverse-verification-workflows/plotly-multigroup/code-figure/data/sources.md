# Source index

`data/src/example-plot.py` is the complete script shown in the figure. `data/src/code_figure.py` is the copied renderer needed to reproduce it. Their SHA-256 digests are recorded in `sources.csv`.
