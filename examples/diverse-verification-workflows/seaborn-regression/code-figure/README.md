# Continuous association producer code

Claim: This is the complete Seaborn producer used to create the neighboring continuous association example plot.

The editable SVG and 2x PNG are two carriers of one ReproFig figure identity. The plotted data are a per-line table of the exact source text, token classes, and rendered coordinates. No inferential statistics apply to this code figure.
